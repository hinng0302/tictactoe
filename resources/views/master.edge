<!DOCTYPE html>
<html lang="en">
<head>
  @include('partials.header')
  <title>
    @section('title')
      Tic Tac Toe
    @endsection
  </title>
</head>
<body>
    @set('username', request.input('username'))
    @set('newregist', request.input('newregist'))
  <section>
    @section('body')
      @if(username)
        <div class="onboard">
          <div class="container">
            <div class="row">
              <div class="col-8">
                <p class="title">Hello {{ username }} ! </p>
              </div>
              <div class="col-3">
                <a href="/newRm?username={{username}}">
                  <button class="btn btn-primary"><span class="title">New Room</span></button>
                </a>
              </div>
            
            </div>
            <hr />
            
            <div class="row">
              <div class="col-9">
                <p class="title">Please select room:</p>
              </div>
              <div class="col-2">
                <form action="/" method="get">
                <input type="hidden" name="username" value="{{username}}">
                  <button class="btn btn-primary" type="submit">refresh</button>
                </form>
              </div>
            </div>
            <hr />
            <div class="message">
              <div class="container">
                @each(rm in rm_lists)
                  @!component('components.room_enterbutton', rm=rm, username=username)
                @endeach
              </div>
            </div>
          </div>
        </div>
      @else
        <div class="container">
          <div class="row">
            <div class="onboard">
              <div class="container">
                  @if(newregist)
                      <div class="row" style="z-index: 50;">
                        <div class="col-12" style="display:block;">
                          @!component('components.alert', id='234')
                        </div>
                      </div>
                  @endif
                <div class="row">
                  <div class="col-12">
                    <img src="/Tic_tac_toe.jpg" style="margin: 10px auto; display: block; height: 400px; width: 400px;">
                  </div>
                </div>
                <div class="row">
                  <form action="{{ route('LoginController.login') }}" method="POST">
                      {{ csrfField() }}
                    <div class="form-group">
                      <label for="username">Login User:</span>
                      <input class="form-control" type="text" id="username" name="username" placeholder="Enter your username" autofocus=1>
                    </div>
                    <div class="form-group">
                      <label for="password">Password:</label>
                      <input class="form-control" type="password" id="password" name="password" placeholder="Enter your password" autocomplete="current-password">
                    </div>
                    <br>
                    <div class="form-group">
                      <div class="col-12 offset-7">
                          <a href="/regist_page"> Register</a>
                        <button type="submit" class="btn btn-primary">Login</button>
                      </div>
                    </div>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      @endif
    @endsection
  </section>
</body>
</html>
