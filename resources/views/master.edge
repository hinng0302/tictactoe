<!DOCTYPE html>
<html lang="en">
<head>
  @include('partials.header')
  <title>
    @section('title')
      Tic Tac Toe
    @endsection
  </title>
</head>
<body>
    @set('username', request.input('username'))
    @set('newregist', request.input('newregist'))
{{--    
    <section>
      --}}
    @section('body')
      @if(username)
        <div class="onboard">
          <div class="container" style="padding-right: 10%; padding-left: 0.1em;">
            <div class="row">
              <div class="col-7">
                <p class="title">Hello {{ username }} ! </p>
              </div>
              <div class="col-5">
                <a href="/newRm?username={{username}}">
                  <button class="btn btn-primary"><span class="title">New Room</span></button>
                </a>
              </div>
            
            </div>
            <hr />
            
            <div class="row">
              <div class="col-7">
                <p class="title">Please select room:</p>
              </div>
              <div class="col-5">
                <form action="/" method="get">
                <input type="hidden" name="username" value="{{username}}">
                  <button class="btn btn-primary" type="submit">refresh</button>
                </form>
              </div>
            </div>
            <hr />
            <div class="message">
              <div class="container" style="padding-right: 10%; padding-left: 0.1em;">
                @each(rm in rm_lists)
                  @!component('components.room_enterbutton', rm=rm, username=username)
                @endeach
              </div>
            </div>
          </div>
        </div>
      @else
        <div class="container">
          <div class="row">
            <div class="onboard">
              <div class="container">
                  @if(newregist)
                      <div class="row" style="z-index: 50;">
                        <div class="col-12" style="display:block;">
                          @!component('components.alert', id='234')
                        </div>
                      </div>
                  @endif
                <div class="row">
                  <div class="col-12">
                    <img src="/Tic_tac_toe.jpg" style="margin: 0.1em auto; display: block; height: 100%; width: 100%;">
                  </div>
                </div>
                <div class="row col-12" style="width: 100%;">
                  <form action="{{ route('LoginController.login') }}" method="POST">
                      {{ csrfField() }}
                    <div class="form-group"><br>
                      <label for="username"></span>
                      <input class="form-control" type="text" id="username" name="username" placeholder="Enter username" autofocus=1>
                    </div>
                    <div class="form-group">
                      <label for="password"></label>
                      <input class="form-control" type="password" id="password" name="password" placeholder="Enter password" autocomplete="current-password">
                    </div>
                    <br>
                    <div class="form-group">
                      <div align="right">
                          <a href="/regist_page"> Register</a>
                        <button type="submit" class="btn btn-primary">Login</button>
                      </div>
                    </div>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      @endif
    @endsection
{{--    
  </section>
    --}}
</body>
</html>
